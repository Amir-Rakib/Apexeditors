var $black_white = $('.black_white'),
    img_width = $('.black_white img').width(),
    init_split = Math.round(img_width / 2);

$black_white.width(init_split);

$('.before_after_slider').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});
$('.before_after_slider1').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider1').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider2').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider2').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider3').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider3').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider4').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider4').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider5').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider5').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider6').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider6').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider7').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider7').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider8').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider8').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider9').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider9').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider10').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider10').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});

$('.before_after_slider11').mousemove(function (e) {
    var offX = (e.offsetX || e.clientX - $black_white.offset().left);
    $black_white.width(offX);
});

$('.before_after_slider11').mouseleave(function (e) {
    $black_white.stop().animate({
        width: init_split
    }, 2000)
});
